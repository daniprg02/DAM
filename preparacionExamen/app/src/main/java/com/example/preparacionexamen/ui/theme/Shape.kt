package com.example.preparacionexamen.ui.theme

import androidx.compose.material3.Shapes

// --- Definición de las Formas de los Componentes ---

// Se crea un objeto `Shapes`.
// Al igual que con la tipografía, no estamos personalizando ninguna forma,
// por lo que simplemente creamos una instancia vacía `Shapes()`.
// Esto le dice a MaterialTheme que use las formas por defecto de Material 3
// (bordes redondeados para botones, tarjetas, etc.).

// Si quisiéramos personalizar las formas, lo haríamos aquí. Por ejemplo:
// val Shapes = Shapes(
//    small = RoundedCornerShape(4.dp),
//    medium = RoundedCornerShape(8.dp),
//    large = RoundedCornerShape(16.dp)
// )
val Shapes = Shapes()
