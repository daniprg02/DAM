<?xml version="1.0" encoding="UTF-8"?>
<persistence version="3.0"
             xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
    https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd">

    <persistence-unit name="miUnidadPersistencia" transaction-type="RESOURCE_LOCAL">
        <!-- Proveedor: Hibernate -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>



        <!-- Usar el paquete correcto -->
        <!-- CLASES -->

        <class>sistemaMatriculas.AlumnoJPA</class>
        <class>sistemaMatriculas.CursoJPA</class>
        <class>sistemaMatriculas.MatriculaJPA</class>
        <exclude-unlisted-classes>true</exclude-unlisted-classes>

        <properties>
            <!-- Conexión a MySQL -->
            <property name="jakarta.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/matriculas?useSSL=false"/>
            <property name="jakarta.persistence.jdbc.user" value="root"/>
            <property name="jakarta.persistence.jdbc.password" value=""/>

            <!-- Configuración de Hibernate -->
            <!-- Cambia esto si MySQL es 5.5-5.7: -->
            <!-- <property name="hibernate.dialect" value="org.hibernate.dialect.MySQL57Dialect"/> -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>


        </properties>
    </persistence-unit>
</persistence>